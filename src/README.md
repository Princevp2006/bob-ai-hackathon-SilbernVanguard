# Source Code

This directory contains the entire source code for the Power Grid Health Dashboard.

## Layout

- `frontend/`: The React, Vite, and Neumorphism CSS frontend. Run with `npm run dev`.
- `backend/`: The Python FastAPI server and Scikit-learn Machine Learning pipeline. Run with `uvicorn api:app`.
